# B9.14 Staging Deployment Log

状态日期：**2026-07-15**

## 当前状态

B9.14 已在真实服务器完成恢复式 staging 部署与验收。

```text
B9.14 = completed for staging
staging_target = https://exam.acgbot.cc.cd
release_commit = 1686c6e27a23029c6cc53c8a22ddb843c3d332d7
service = bkyexam-practice-api.service active
nginx = active
postgresql = active
deployment_evidence = ready=true, 14 pass / 0 warn / 0 fail
```

说明：这证明当前分支可在目标服务器和真实题库数据上运行；它不等于 PR 已 review/merge，也不等于已接入外部监控告警或完成正式压测。

## 目标环境

- 公开访问域名：`exam.acgbot.cc.cd`
- Cloudflare 代理域名：`acgbot.cc.cd` / `exam.acgbot.cc.cd`
- 服务器 origin IP：`47.88.33.54`
- 可 SSH 用户：`root@47.88.33.54`
- 服务器系统：Debian 12
- Node：`v24.18.0`
- npm：`11.16.0`
- PostgreSQL：15.18
- Nginx：1.22.1
- 部署目录：`/srv/bkyexam-practice-platform`
- 题库目录：`/srv/bkyexam-data/import-20260705153307/questionbank`
- 备份/证据目录：`/srv/bkyexam-backups/b9.14-20260715080815`

## 部署前审计快照

部署前服务器上运行的是旧 `main`：

```text
/srv/bkyexam-practice-platform
branch = main
HEAD = f45f69b fix: restore completed practice answer details
service = bkyexam-practice-api.service active
api = 127.0.0.1:3000
nginx = exam.acgbot.cc.cd -> /srv/bkyexam-practice-platform/apps/web/dist and /api -> 127.0.0.1:3000
```

数据库部署前摘要：

```text
students = 13
classifications = 2941
questions = 89922
question_options = 154899
bank_mappings = 2662
practice_sessions = 22
admin_users = table absent
schema_migrations = table absent
```

旧学生账号均为无密码旧账号，示例包括：`001`、`114`、`smoke-import-check`、`verify-filter-search`、`昨日今日明日`。

## 中断与恢复

首次部署脚本在 `npm ci` 阶段后服务器一度无法交互，表现为 SSH banner timeout / HTTP 无响应。用户重启后恢复 SSH。

恢复后审计确认：

- 备份目录已生成：`/srv/bkyexam-backups/b9.14-20260715080815`
- 部署前 DB dump 已生成：`bkyexam_practice.before.dump`（约 31.7 MB）
- Git 已切到部署分支，但 `npm ci` 未完成，`node_modules/fastify` 缺失
- API service 因依赖缺失处于失败/停止状态
- `/etc/bkyexam-practice-api.env` 当时尚未写入 B9.14 新 guardrail 参数

恢复策略：停止 API service，重置到最新远端分支 `origin/codex/practice-platform-stabilization`，续跑安装、构建、迁移、导入、账号迁移和 smoke。

## 部署执行结果

部署 commit：

```text
1686c6e27a23029c6cc53c8a22ddb843c3d332d7
```

安装/构建/数据库步骤：

| Step | Result |
| --- | --- |
| `npm ci --prefer-offline --no-audit --no-fund` | PASS |
| `npm run build` | PASS |
| `npm run db:migrate -w @bkyexam-practice/api` | PASS，应用 `0001`–`0011` |
| `npm run import:db -w @bkyexam-practice/api -- /srv/bkyexam-data/import-20260705153307/questionbank` | PASS |
| `npm run db:smoke -w @bkyexam-practice/api` | PASS |

题库导入/校验结果：

```json
{
  "classifications": 2941,
  "questions": 89922,
  "options": 154899,
  "skippedOptions": 25424,
  "bankMappings": 2662
}
```

## 生产运行参数

`/etc/bkyexam-practice-api.env` 已更新并保存副本到备份目录。

关键参数：

```text
NODE_ENV=production
USE_DATABASE=true
COOKIE_SECURE=true
STUDENT_LEGACY_PASSWORDLESS_LOGIN_ENABLED=false
STUDENT_LOGIN_MAX_FAILURES=10
STUDENT_LOGIN_FAILURE_WINDOW_MINUTES=30
STUDENT_LOGIN_LOCK_MINUTES=15
ADMIN_SESSION_TTL_HOURS=8
ADMIN_IMPORT_ALLOWED_ROOTS=/srv/bkyexam-data
ADMIN_IMPORT_ENABLE_WRITE=false
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=600
CSRF_ORIGIN_CHECK_ENABLED=true
CSRF_ALLOWED_ORIGINS=https://exam.acgbot.cc.cd;https://acgbot.cc.cd
```

`DATABASE_URL` 与 `COOKIE_SECRET` 被保留/强化并只存在服务器 env 文件中，未写入 Git。

## 身份迁移与账号初始化

旧账号迁移：

- dry-run 候选：13 个旧无密码学生账号
- apply 迁移：13/13 成功
- 撤销旧 session：总计 15 个未过期旧 session
- 旧账号保留：是，未删除、未禁用
- 明文临时密码：只写入服务器受限 CSV，不在日志/对话输出

正式账号初始化：

| Account | Result |
| --- | --- |
| 管理员 `admin` | 已创建/刷新，`super_admin`，displayName=`系统管理员` |
| 学生 `202502040201`–`202502040230` | 已创建/刷新 30 个 |
| className | `2班` |
| groupName | `NULL` |
| passwordResetRequired | `true` |

最终账号摘要：

```text
students.total = 43
students.active = 43
students.disabled = 0
legacy_passwordless = 0
password_reset_required = 43
formal 2班 accounts = 30
admin_users.total = 1
admin_users.active = 1
```

凭据文件：

```text
/root/bkyexam-credentials/LATEST
/root/bkyexam-credentials/bkyexam-b9.14-credentials-20260715093217.csv
/srv/bkyexam-backups/b9.14-20260715080815/legacy-student-password-migration-credentials.csv
```

注意：`20260715093056` 那一份账号 CSV 是修复 Unicode 字段前生成的旧文件；最终以 `/root/bkyexam-credentials/LATEST` 指向的文件为准。

## Production Gate

目标数据库完整 production gate：

```text
npm run ops:production-gate -- --sample-limit=50
ok = true
```

结果摘要：

- production env：PASS
- `DATABASE_URL` present：PASS
- `USE_DATABASE=true`：PASS
- `COOKIE_SECRET` production-grade：PASS
- `COOKIE_SECURE=true`：PASS
- legacy passwordless disabled：PASS
- rate limit enabled：PASS
- CSRF origin check enabled：PASS
- admin bootstrap secrets absent：PASS
- true import write disabled by default：PASS
- legacy passwordless accounts migrated：PASS (`legacyPasswordlessStudents=0`)
- locked students：PASS (`0`)
- password reset queue：WARN (`43` students require password reset/change)

WARN 是预期结果：本阶段所有旧账号和新建学生均要求首次登录后改密。

证据文件：

```text
/srv/bkyexam-backups/b9.14-20260715080815/production-gate-clean.json
```

## HTTP / Functional Smoke

公共 HTTPS 目标：`https://exam.acgbot.cc.cd`

smoke 覆盖：

| Check | Status | Approx latency |
| --- | ---: | ---: |
| `GET /api/health` | 200 | 85 ms |
| `GET /api/health/readiness` | 200 | 89 ms |
| `GET /api/health/metrics` | 200 | 83 ms |
| `GET /api/banks` | 200 | 946 ms |
| student login `202502040201` | 200 | 159 ms |
| `GET /api/auth/me` | 200 | 82 ms |
| `POST /api/practice/sessions` limit 5 | 200 | 169 ms |
| admin login `admin` | 200 | 217 ms |
| `GET /api/admin/me` | 200 | 89 ms |

验证点：

- readiness 中 API 与 database 均 `ok=true`
- `/api/banks` 返回真实题库映射，首个题库有 UUID bankId
- 学生 `202502040201` 登录成功，`className=2班`，`passwordResetRequired=true`
- 练习 session 创建成功，返回 5 题
- 管理员 `admin` 登录成功，`roles` 包含 `super_admin`
- smoke 脚本不打印密码

证据文件：

```text
/srv/bkyexam-backups/b9.14-20260715080815/http-functional-smoke.json
/srv/bkyexam-backups/b9.14-20260715080815/perf-smoke.json
```

## Deployment Evidence CLI

证据输入：

```text
/srv/bkyexam-backups/b9.14-20260715080815/deployment-evidence-input.json
```

校验输出：

```text
/srv/bkyexam-backups/b9.14-20260715080815/deployment-evidence-report.json
ready = true
summary = 14 pass / 0 warn / 0 fail
```

远端 CI 证据：

```text
PR #2 = https://github.com/Fuck-GH-Admin/bkyexam-practice-platform/pull/2
Workflow run = https://github.com/Fuck-GH-Admin/bkyexam-practice-platform/actions/runs/29380130674
headSha = 1686c6e27a23029c6cc53c8a22ddb843c3d332d7
quality = success
postgres-integration = success
```

`main` branch protection 仍为：required status checks `quality` / `postgres-integration`，required approving reviews `1`，admin enforcement enabled。

## 服务状态与资源

最终服务：

```text
bkyexam-practice-api.service = active
nginx = active
postgresql@15-main.service = active
```

API service：

```text
ExecStart = /usr/bin/node apps/api/dist/index.js
listening = http://127.0.0.1:3000
memory ≈ 55–93 MB during smoke
```

服务器资源快照：

```text
RAM total ≈ 1.6 GiB
RAM available ≈ 615 MiB after smoke
Swap total = 4.0 GiB
Swap used = 0
Disk / = 40G total, 18G used, 20G available, 48%
```

近 10 分钟 API journal 未见 error/warn/fail/exception/ERR_。

## 尚未完成 / 不应误读

B9.14 完成的是 **staging 部署验证**，不是完整生产发布闭环。

仍未完成：

- PR #2 仍需 owner/reviewer review 与 merge 决策。
- 外部监控/告警未接入。
- 只做了轻量 smoke 性能证据，未做系统性压测。
- 管理平台前端尚未设计/实现。
- 学生正式前端仍需在后端边界稳定后重新设计。
- 凭据交付、真实学生名单、班级/分组规则还需运营确认。

## 下一步建议

1. 把本日志、状态页和 evidence runbook 提交并推送，触发 PR CI。
2. 确认 PR #2 是否进入 review/merge。
3. 若要继续后端，建议做 B9.15：外部监控/告警最小闭环、systemd/nginx runbook、简单压测脚本、备份恢复实机演练复核。
4. 前端不建议马上开做最终视觉；等管理平台信息架构、学生端必须功能和账号运营流程再审一轮后再开始正式设计。
